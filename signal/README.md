Signal is a standalone BI mini-games app (grain, star schema, SCD2, join traps).

It is not Vynio. The portfolio tab at `/en/signal` and `/fr/signal` iframes the built copy in `/signal-app/`.

Pass `?locale=en|fr` and `?theme=light|dark` from the parent page.
